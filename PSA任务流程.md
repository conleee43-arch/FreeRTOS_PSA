
对 FreeRTOS 的任务和状态机规划草稿。

---

## 1. 测量状态机的终极演进（单次执行模式）

内阻计算任务（`Calc_Control_Task`）不再是死循环，而是一个“单次触发 $\rightarrow$ 任务自挂起”的架构：

```
[1. 上电等待安全]
       │ (安全任务通知)
       ▼
[2. 设定 3A 并大跨度等待稳定]
       │ (100ms 跨步判定稳定 -> 锁存 U1, I1)
       ▼
[3. 设定 2A 并大跨度等待稳定]
       │ (利用已有基础快速判定 -> 锁存 U2, I2)
       ▼
[4. 计算内阻并配置 IOC 最大电流]
       │ (配置完成，发送串口数据)
       ▼
[5. 进入常态监控 / 任务无限期挂起] ◄───┐
       │                               │ (只要系统安全，一直停在此处)
       └───────(维持10ms轮询监控)───────┘

```

### 状态机的关键行为转变：

* **2A的稳定判定更敏锐**：由于“3A稳定则2A必然存在”，状态机在进入2A阶段时，不需要像3A那样做极其保守的长跨度等待，可以适当缩短消抖计数值，从而快速抓取 $U_2, I_2$。
* **计算后交出控制权**：计算出内阻后，任务立即通过 DAC 将 `IOC` 设定为系统允许的最大输出电流（例如根据内阻动态调整的保护电流）。
* **进入常态监控**：配置完成后，该状态机跳转到一个特殊的 `STATE_MONITOR`（监控状态）。在这个状态下，它不再去改动 DAC（不测3A/2A），而是降级为一个纯粹的“辅助监控器”，每 10ms 配合主任务盯着当前的电压电流。

---

## 2. 什么是“别的情况发生”？（系统重启与重置逻辑）

你提到“除非有别的情况发生”才会重新测 3A。在 FreeRTOS 架构下，这些别的情况通常指**系统的安全状态发生了降级与恢复**：

### 情况 A：突发过温、过压跳闸（系统保护）

1. 运行过程中突发过温或过压，最高优先级的 `Emergency_Task` 介入。
2. 安全任务强行拉低 `OF_EN = 0`，并将 `IOC`（DAC）强行清零。
3. **关键联动**：安全任务通过 FreeRTOS 接口，**强制将内阻计算任务的状态机重置回状态 1（上电等待安全）**。

### 情况 B：环境恢复，重新开机（系统复位测量）

1. 经过一段时间，温度和电压回落到安全迟滞线以下，并且渡过了 2 秒的观察期。
2. `Emergency_Task` 认为可以重新工作，拉高 `OF_EN = 1`。
3. 安全任务再次向内阻计算任务发出“安全通知”。
4. 内阻计算任务被重新激活，**像刚上电时一样，重新开始设定 3A、2A 并重新计算内阻**。

这就是闭环的“别的情况”。通过这种设计，内阻测量与系统的物理保护状态达成了完美的一致性。

---

## 3. PSA 控制系统 FreeRTOS 最终架构全景总结

经过我们多次针对硬件真实物理特性的对齐，这套系统的软件架构已经达到了可以直接指导工程落地的完备度。

### 📊 任务规划与最终职能表

| 任务名称 | 优先级 | 周期/触发 | 本质角色与终期职责 |
| --- | --- | --- | --- |
| **`Emergency_Task`** | **最高** | 1ms~2ms 轮询 | **安全最高统帅**：管控对外输出总闸 `OF_EN`。采用双阈值迟滞状态机。一旦跳闸，斩断 `OF_EN` 和 `DAC`，并强行重置计算任务；一旦满足 2 秒冷启动恢复，开启 `OF_EN` 并重新激活测量。 |
| **`Sample_Filter_Task`** | **中高** | 2ms~5ms 定时 | **数字数据底座**：ADC+DMA 循环扫描 6 路原始值（含电压电流、输入、温度及 REF）。利用内部 REF 实时校正 $V_{dda}$ 消除噪声，进行软件滤波，并提供初级超限报警。 |
| **`Calc_Control_Task`** | **中** | 10ms 定时 | **自适应测量与配置器**：上电或复位后**仅单次执行**内阻计算状态机。在 100ms 大跨度视窗下自适应等待 3A、2A 物理建立。计算出内阻后，配置 `IOC` 最大输出电流，随后进入常态监控，直到被安全任务强行重置。 |
| **`Uart_Comm_Task`** | **最低** | 事件驱动（队列） | **异步信息窗口**：通过 FreeRTOS 队列接收单次内阻计算报告或常态数据，利用 **串口 TX DMA** 异步送出，绝不占用和阻塞任何核心控制时间。 |

### 🔄 核心数据与控制流闭环图

```
                       【 上电 / 保护后恢复 】
                                  │
                                  ▼
                        [Emergency_Task 统帅]
                                  │
                          (发放安全准入通知)
                                  ▼
                      [Calc_Control_Task 状态机]
                                  │
             ┌────────────────────┴────────────────────┐
             ▼                                         ▼
      【 1. 设定 3A 】                          【 2. 设定 2A 】
 (100ms 跨步自适应等稳)                    (基于3A基础，敏锐等稳)
             │                                         │
             └────────────────────┬────────────────────┘
                                  ▼
                         【 3. 计算内阻 】
                                  │
                       (根据内阻锁定 IOC 最大电流)
                                  ▼
                       【 4. 锁定常态监控 】 ──► (不再测 3A/2A)
                                  │
         ┌────────────────────────┴────────────────────────┐
         ▼ (若发生突发过温/过压)                             ▼ (若一切正常)
[Emergency_Task 瞬间强刷]                               [持续维持稳定输出]
 │ 1. 拉低 OF_EN = 0
 │ 2. 清零 DAC = 0
 │ 3. 强行将测量状态机退回起点
 └───────►【 等待环境冷却降压 】

```

